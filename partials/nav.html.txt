REFERENCE ONLY (not shipped): shared nav markup lives duplicated at the top of every page body, right after <body>, ending at </header>. Shared footer markup lives right before the closing </body> scripts. See index.html for the canonical copy used across all pages. Active nav differences: features.html/integrations.html/security.html/changelog.html open under a 'Product' concept but only pricing.html marks .nav__link.is-active on the Pricing-equivalent link when present; most pages leave top-level links unmarked except the page's own top-level link (Customers/Blog/About) which gets class="nav__link is-active".
